# 疑問文トレーナー — 要件定義 & 設計書

> **位置づけ**：実装（Cursor）の前提を固める文書。「何を作るか・どう作るか」を定義する。
> **関連ドキュメント（正本セット）**：
> - 設計の根拠（なぜその分岐があるか）→ `question-trainer-structure-map.md`
> - 生成ロジックの分岐図 → `question-trainer-decision-tree.svg`
> - **UIの視覚的正本**（画面構成・配色・部品分割）→ `question-trainer-prototype.html`
> - 実装の引き渡し指示（既存トレーナー規約に合わせた差分）→ `question-trainer-cursor-work-request.md`
>
> 本書はこれらを実装可能な仕様に束ねる上位文書。迷ったら本書 → 各正本の順に参照する。
> **初版スコープ**：出題 → 自力翻訳 → 模範解答＋根拠の表示 → MDエクスポート。**AIによる添削はv1では行わない**（MDをデスクトップClaude Projectsへ渡して添削を得る運用）。

---

## 1. 目的とゴール

- フィリピン留学中の実用に向け、**疑問文＋時制を瞬発的に産出するスキル**を鍛える。
- 構造トレーナーと**同じフォーマット**（日本語提示→英訳入力→フィードバック）を踏襲しつつ、対象を疑問文に特化。
- **軽量トークン運用**を最優先（後述の通り1セッション＝1 APIコール）。
- GitHub Pages で個人運用。

非ゴール（v1スコープ外、将来）：瞬発ループ（速度計測・タイマー）、AI添削、音声・IPA。

---

## 2. 機能要件（FR）

| ID | 要件 | 詳細 |
|---|---|---|
| FR-1 | STEP選択出題 | 操作ベース7 STEP（structure-map §8-1）から1つ選んで出題 |
| FR-2 | 総合モード | **指定したSTEP集合からランダム出題**（複数STEP選択→その集合内で各問ランダム）。弱点強化用。全STEPの無条件ランダムではない |
| FR-3 | 5問一括出題 | 1セッション＝**5問**。日本語のみを一括提示（模範解答は隠す） |
| FR-4 | 解答入力 | 各問にテキスト入力欄。未入力でも答え合わせ可 |
| FR-5 | 答え合わせ | 「答え合わせ」操作で**5問の模範解答を一括表示**。自分の解答と並置。表示は最小限（§6-3） |
| FR-6 | 答え文（質問文＋回答文） | 各問に 自分の回答／模範解答（質問文）／模範解答（回答文）を表示。内部メタ（狙い・構造分解・操作）は**画面に出さず**MD出力に回す |
| FR-7 | MD出力（1問ずつ・貼付プロンプト形式） | **問題ごとに**MDを出力（コピー）。MDは**そのままProjectsに貼るだけで添削が走る指示文**を内包（§10）。①自分の回答の添削 ②模範解答が正しい理由 ③その他の表現、をClaudeに解説させる |
| FR-8 | シーン文脈 | 出題日本語にシーン（structure-map §9）の文脈を薄く着せる。話題は固定せずランダムサンプリング |
| FR-9 | 答え文（該当STEP） | STEP4の when/where/why/how は模範解答に「答え文」も併記（`answerSense`固定） |

**v1で実装しないこと（明示）**：AI添削（FR外）／スコアリング／履歴のサーバー保存／タイマー・速度ログ。

---

## 3. 非機能要件（NFR）

| ID | 要件 | 方針 |
|---|---|---|
| NFR-1 | トークン効率 | **1セッション=1 APIコール**。5問の「日本語＋メタ＋模範解答＋根拠」を1リクエストで一括生成（§7）。添削の別コールを持たない |
| NFR-2 | モデル | `claude-sonnet-4-5`（既存トレーナーと同系・同文字列に合わせる。grading-prompt.md 準拠）。**問題作成と模範解答を同一コールで生成** |
| NFR-3 | 静的ホスティング | GitHub Pages（SPA）。ビルドは React |
| NFR-4 | APIキー秘匿 | フロントに鍵を置かない。**Google Apps Script を薄いプロキシ**として既存スタックと同様に経由（v1はSheets不要・ステートレス可） |
| NFR-5 | レスポンス安定化 | JSONのみ返却＋assistantで `[` をprefill（grading-prompt.md と同方式） |
| NFR-6 | 不変条件の保証 | ハードな文法不変条件は**プロンプト依存にせずコードで後検証**（§8）。違反問はクライアントで除外/再要求 |
| NFR-7 | オフライン | 不要（v1）。生成は都度オンライン |

---

## 4. システム構成

```
[React SPA / GitHub Pages]
   │  (1) セッション開始：STEP集合・問数=5・シーンプール送信
   ▼
[GAS プロキシ]  ── APIキー付与 ──▶ [Claude API  claude-sonnet-4-5]
   ▲                                        │ 1コールで5問ぶん
   │  (2) JSON配列（5 items：jp/meta/en/nuance/parts/thread）
   ▼
[React 状態管理]
   ├─ 出題フェーズ：item.jp のみ表示
   ├─ 答え合わせフェーズ：item.en + 根拠を開示
   └─ エクスポート：MD生成（ローカルDLのみ）
```

- **バックエンドの状態は持たない**（v1）。生成結果はクライアントのメモリ内のみ。リロードで破棄でよい。
- 将来、履歴・SRSを足す場合に Sheets を追加する余地を残す（v1では使わない）。

---

## 5. データモデル

### 5-1. 出題アイテム（生成APIの返却要素）

```jsonc
{
  "id": "q1",                     // セッション内連番
  "step": 3,                      // 1..7（structure-map §8-1）
  "sceneTag": "道聞き",            // シーンプール由来（彩り・MECE非保証）
  "functionTag": "情報を得る",      // 機能軸（任意）
  "jp": "あなたは昨日どこでそのカバンを買いましたか？",  // 出題文（一意性担保）
  "targetGap": "where",           // 聞くスロット固定（必須）
  "answerSense": null,            // when/where/why/how のみ：point|clause|cause|purpose|means|manner|state|degree
  "en": "Where did you buy the bag yesterday?",   // 模範解答（100点）
  "nuance": "did挿入後の本動詞は原形。yesterday は文末。",  // 模範のポイント
  "thread": "糸1+糸2",             // 糸1 / 糸2 / 糸1+糸2
  "answerSentence": null,         // STEP4等で答え文を併記する場合のみ（例 "I bought it at the mall."）
  "parts": [                      // 構造分解（grading-prompt.md と同じ X/Y/Z/V）
    { "role": "Z", "text": "Where", "note": "場所を問うwh" },
    { "role": "V", "text": "did buy", "note": "do挿入・原形" },
    { "role": "X", "text": "you", "note": "主語" },
    { "role": "X", "text": "the bag", "note": "目的語" },
    { "role": "Z", "text": "yesterday", "note": "時の副詞" }
  ]
}
```

役割コード（X 名詞役 / V 動詞 / Y 形容詞役 / Z 副詞役）は grading-prompt.md の定義と一致させる。

### 5-2. セッション状態（クライアント）

```jsonc
{
  "mode": "step" | "combined",
  "selectedSteps": [3,4,6],       // combined は集合、step は単一
  "phase": "answering" | "review",
  "items": [ /* 5 × 出題アイテム */ ],
  "attempts": { "q1": "Where did you bought...", ... }
}
```

---

### 5-3. 表示用語の方針（内部コード ↔ 平易表記）★①

**サイト表示・MD出力では、独自用語「糸／糸1／糸2」「X/Y/Z/V」を一切使わない**（一般に馴染みがなく初見ユーザーに伝わらないため）。データ内部のコードは保持しつつ、ユーザーに見せる箇所は下表の**平易表記に変換**する。冗長でも、初見で意味が分かる説明にする。

| 内部コード | 平易表記（表示・MD用） |
|---|---|
| thread `糸1` | 助動詞を主語の前に出す |
| thread `糸2` | 疑問詞を文頭に動かす |
| thread `糸1+糸2` | 助動詞を前に出し、疑問詞を文頭に動かす |
| thread `糸1解除` | 助動詞は前に出さない（ふつうの語順のまま） |
| role `X` | 名詞のはたらき（主語・目的語など）｜短縮: 名詞 |
| role `V` | 動詞 |
| role `Y` | 名詞を説明する語句（形容詞・関係詞節など）｜短縮: 名詞修飾 |
| role `Z` | 時・場所・理由などを表す語句（副詞・副詞節など）｜短縮: 副詞 |

- 生成プロンプトが返す `nuance` と `parts[].note` も**平易な日本語**で書かせる（§7で「糸/XYZ という語を使わない」と明示）。
- `thread`・`role` のコード値はJSONに残してよい（将来用）。**表示・MDへ出すときに必ず変換**する。

---

## 6. 画面・UXフロー

構造トレーナーと同じ操作感。**3フェーズ**（select → answer → review）。一括エクスポート画面は廃止し、MD出力は答え合わせ画面で**問題ごと**に行う。表示用語は §5-3 の平易表記に従う（糸/XYZ を出さない）。

1. **STEP選択**
   - STEP個別（1つ選択）／総合（複数選択＝指定STEP集合）をトグル。
   - 各STEPに一行説明（平易表記。糸/XYZ を使わない）。STEPカードの「糸タグ」は廃止。
2. **出題（answering）**
   - 5問の日本語を一括表示。各問に入力欄。模範解答は非表示。
   - 「答え合わせ」ボタン（未入力があっても押下可、確認なし）。
3. **答え合わせ（review）** — §6-3 の最小要素のみ
   - 「次の5問」で再生成（新しいAPIコール）。

### 6-3. 答え合わせカードの表示要素（③・これで十分）

各問カードに出すのは次のみ。**赤枠で示された内部メタ（狙い・answerSense・操作・構造分解・ポイント）は画面に出さない**（MD出力に内包する）。

- ヘッダ：問番号 / STEP名（平易）/ シーン
- 出題（日本語）
- **自分の回答**
- **模範解答（質問文）**
- **模範解答（回答文）** — 答え文ありSTEPのみ（無ければ非表示）
- **MD出力ボタン**（この1問ぶんを出力／コピー）

UX文言は学習者表現で（「答え合わせ」「次の5問」「この問題をMDで出力」）。

---

## 7. 生成API仕様（1コール）

**問題作成と模範解答を1リクエストで生成する。** 添削用の別コールは持たない。
品質・トークンの両面で最適なので、**問題（日本語＋メタ）と模範解答＋根拠は同一レスポンス内でセット生成する**（生成を分割しない／同じコンテキストで作るため問題と模範解答が必ず整合する）。「出題時は日本語のみ表示・答え合わせ時に模範解答を開示」は生成後の**表示制御**であって、生成を2回に分けるものではない。

### 7-1. 呼び出しパラメータ

| 項目 | 値 |
|---|---|
| model | `claude-sonnet-4-5` |
| max_tokens | `8192` |
| 1コールの生成数 | **5問** |
| prefill | assistant メッセージで `[`（JSON配列の先頭固定） |

### 7-2. System プロンプト（要旨）

```
あなたは英語教育の専門家であり、疑問文産出ドリルの作問者です。
学習者は日本語を読んで瞬発的に英語の疑問文を作る訓練をしています。
指定された STEP（操作）とシーンに沿って、出題用の日本語文と、その模範解答・根拠を生成してください。
必ず有効なJSONのみを返してください。マークダウンや説明文は一切含めないでください。

JSONの厳守ルール（grading-prompt.md と同一）:
- 文字列値はダブルクォートのみ使用（シングルクォート不可）
- nuance/note 内で英文・英単語を引用するときは『』を使い、ダブルクォート(")は使わない
- 末尾カンマ禁止 / 生の改行禁止（必要なら \n）

作問の絶対ルール（取りこぼし厳禁）:
- 1文につき疑問詞は1つ（2つ同時に問わない）
- 糸1（助動詞前置）は3機構を正しく使い分ける：本動詞が be→be前出 / 既存助動詞→それを前出 / 一般動詞で助動詞なし→do/does/did を挿入（be・既存助動詞があるとき do は使わない）
- 主語を問う wh では糸1を起動しない（語順そのまま・時制は本動詞の活用に乗せる）
- 間接疑問では従属節の糸1を解除する（do を消し平叙語順）。疑問詞は節頭に保持
- 複雑な関係詞修飾で疑問文を飾り立てない（瞬発訓練の阻害）

一意性ルール（最重要）:
- 出題日本語は、targetGap で指定した1つのスロットだけが答えになるよう、曖昧さなく書く
- targetGap が when/where/why/how のときは answerSense も固定し、その意味に一意に収束する日本語にする
  （例：why で purpose を狙うなら「何のために」と書き、cause と混ざらないようにする）
```

### 7-3. User プロンプト（テンプレート）

```
次の条件で疑問文ドリルを {{COUNT}} 問つくってください。

- 対象STEP: {{STEPS}}            // 単一 or 集合（combined）。各問はこの集合からSTEPを割り当てる
- STEP定義:
  {{STEP_DEFS}}                  // 選ばれたSTEPの「観点・例・対応軸」を structure-map §8-1 から抜粋して注入
- 使用してよいシーン（彩り・話題は固定しない／毎問ランダムに1つ選ぶ）:
  {{SCENE_POOL}}                 // structure-map §9 の A/B/B'/C を注入
- 時制は現在・過去・be・未来・現在完了の核5時制から各問に割り当てて散らす
- 各問に必ず targetGap を設定。when/where/why/how は answerSense も設定
- STEP4（副詞wh）の問題は answerSentence（模範の答え文）も付ける

返却形式（JSONのみ、{{COUNT}}要素の配列。各要素のスキーマは §5-1 と同一）:
[
  {
    "id": "...", "step": 0, "sceneTag": "...", "functionTag": "...",
    "jp": "...", "targetGap": "...", "answerSense": null,
    "en": "...", "nuance": "...", "thread": "...", "answerSentence": null,
    "parts": [ { "role": "X|V|Y|Z", "text": "...", "note": "..." } ]
  }
]

nuance / note ルール:
- 模範解答がなぜその形になるかを 1〜2文で。語順・時制（どの助動詞を前に出したか／主語をたずねるwhでは助動詞を前に出さない 等）のうち該当点に触れる
- why/how/when/where は answerSense（原因/目的・手段/様態・時点/節 等）にも1語触れる
- **出力（nuance・parts[].note）には独自用語「糸／糸1／糸2」「X/Y/Z/V」を使わない。** 平易な日本語で書く（例: ×「糸1を解除し」→ ○「助動詞を前に出さず、ふつうの語順にして」／ ×「Zが文頭」→ ○「時を表す語句を文頭に置き」）。`role`/`thread` のコードはJSONフィールドにのみ残し、説明文には日常語を使う
```

### 7-4. 返却の後処理

- `[` prefill 前提でパース。要素数 = 5 を検証。
- §8 のバリデーションを通す。違反アイテムは除外し、不足分は再リクエスト（最大1回）。

---

## 8. 不変条件のコード強制（生成後バリデーション）

プロンプト遵守は確率的なので、ハードな不変条件は**コードで検証**する（NFR-6）。各アイテムに対し：

| 検査 | 条件 | 失敗時 |
|---|---|---|
| 疑問詞は1つ | `en` 内の wh語が高々1つ（間接疑問の従属節whは別扱い） | 除外・再要求 |
| do挿入の排他 | `en` に「do/does/did + (be/既存助動詞)」の共起がない（×Do you can…） | 除外・再要求 |
| 主語whの糸1禁止 | `targetGap=subject` のとき先頭に do/be/助動詞の前置がない | 除外・再要求 |
| 間接の糸1解除 | step=6 の従属節が平叙語順（疑問詞直後に助動詞前置がない） | 除外・再要求 |
| 末尾は ? | `en` が `?` で終わる | 整形 |
| メタ必須 | `targetGap` 必須／when・where・why・how は `answerSense` 必須 | 除外・再要求 |
| STEP整合 | `step` が selectedSteps に含まれる | 除外 |

> 検査は構文ベースの軽量チェックでよい（完全な構文解析は不要）。目的は「明白な型崩れの排除」。

---

## 9. STEP定義の注入元（structure-map §8-1 ミラー）

| STEP | 観点 | 糸 | targetGap候補 |
|---|---|---|---|
| 1 | Yes/No（糸1のみ・3機構） | 糸1 | yesno |
| 2 | wh・主語を聞く（糸2のみ） | 糸2 | subject |
| 3 | wh・主語以外を聞く（糸1+糸2） | 糸1+糸2 | object/complement/when/where/why/how |
| 4 | 副詞wh＋答え文 | 糸1+糸2 | when/where/why/how（answerSense必須・answerSentence付与） |
| 5 | 限定詞型・前置詞残留 | 糸1+糸2 | which/what/whose+名詞・前置詞残留 |
| 6 | 間接疑問（糸1解除・二重疑問） | 糸1解除 | 従属節内のgap＋主節トリガー |
| 7 | 選択・否定疑問 | 糸1 | alternative/negative |

---

## 10. MD出力仕様（1問ずつ・貼付プロンプト形式）★②

**問題ごと**にMDを出力する（コピー主体。任意でダウンロード可）。出力MDは記録であると同時に、**そのままProjectsに貼るだけで添削が走る指示文**を内包する。指示は構造トレーナーの採点プロンプト（`grading-prompt.md` / `prompts/index.js buildCheckPrompt` の feedback設計）を平易化して内包し、次の3点をClaudeに解説させる。

1. 自分の回答の添削（文の組み立て＝語順・時制・助動詞・疑問詞の使い方に沿って、正誤を具体的に）
2. 模範解答がなぜ正しいと言えるのか（語順・時制・表現の選択根拠を初学者向けに）
3. その他の表現方法（より自然な言い方・別解）

ファイル名（DL時）：`qt_step{step}_q{n}_{yyyymmdd-hhmm}.md`。**用語は平易表記**（糸/XYZ を出さない。§5-3で変換）。

```markdown
# 疑問文トレーナー 添削依頼（1問）

あなたは英語教育の専門家です。下の1問について、日本語で次の3点を解説してください。

1. **私の回答の添削** — 文の組み立て方（語順・時制・助動詞の有無・疑問詞の使い方）に沿って、
   正しい点と誤っている点を具体的に。誤りは「何が」「なぜ」「正しくはどうするか」をセットで。
2. **模範解答がなぜ正しいのか** — 語順・時制・表現の選択根拠を、初学者にも分かる言葉で。
   私の回答との違いにも触れる。
3. **その他の表現方法** — 同じ意味をより自然に言う別の言い方や、場面で使える言い換えがあれば。

---

## 出題
{{jp}}

## 私の回答
{{attempt or （未入力）}}

## 模範解答
- 質問文: {{en}}
- 回答文: {{answerSentence or （この問題に回答文はありません）}}

## 参考（解説の手がかり・平易表記）
- 出題のねらい: {{targetGap を平易表記。例: 「いつ」をたずねる}}
- 模範解答のポイント: {{nuance（平易）}}
- 文の組み立て: {{parts を平易ラベルで「・名詞: you（主語）/ ・動詞: did buy（過去・do を前に）…」と整形}}
```

> 内部メタ（targetGap/answerSense/操作/構造分解）は**この参考ブロックに平易表記で**埋め込む。画面（§6-3）には出さない。ProjectsにはこのMDを貼るだけで、上の指示に従い3点の解説が返る。

---

## 11. 受け入れ条件（Acceptance Criteria）

- [ ] STEP個別・総合（指定STEP集合からランダム）の両モードで5問を出題できる
- [ ] 出題フェーズで模範解答が見えない／答え合わせで一括開示される
- [ ] 1セッションのClaude呼び出しが**1回**である（添削の別コールがない）
- [ ] 各問に targetGap が入り、when/where/why/how には answerSense が入る
- [ ] §8 のバリデーションを全アイテムが通る（違反は除外/再要求される）
- [ ] STEP4で模範の答え文（回答文）が表示される
- [ ] **①** 画面・MDのどこにも「糸／X/Y/Z/V」表記が出ない（内部コードは平易表記に変換）。`nuance`/`note` も平易
- [ ] **②** MDは**問題ごと**に出力でき、Projectsに貼るだけで「添削／模範の理由／その他表現」の3点解説が返る指示文を内包
- [ ] **③** 答え合わせカードの表示は §6-3 の最小要素のみ（内部メタは非表示）
- [ ] APIキーがフロントに露出しない（GAS経由）

---

## 12. スコープ外・将来（タグ付き）

- **瞬発ループ（速度計測・タイマー・無思考モード）** — 第2フェーズ。形になってから導入。
- **AI添削の内製** — v1はMD→デスクトップClaude運用。将来 grading-prompt.md 流用で追加可。
- **答え文の when/where 拡張詳細・what慣用** — structure-map 第2層。
- **how+α・pied-piping・受動疑問・backshift・仮定法系の問い** — 第2層。
- **履歴/SRS（Sheets連携）** — v1はステートレス。

---

## 13. 確定事項（実装方針）

1. **Claude APIキーは GAS で管理**：フロントは鍵を持たず、GASプロキシ経由でClaude APIを呼ぶ（NFR-4）。
2. **生成は1コール・セット生成**：問題（日本語＋メタ）と模範解答＋根拠を**同一の1リクエストで一緒に生成**する（品質・トークンとも最適）。分割しない。表示のみ出題時／答え合わせ時の2段に分ける（UI制御）。
3. **再生成は都度5問**：「次の5問」は毎回フル5問を新規生成（=毎回1コール）。先読みプールは持たない。
4. **総合モードは完全ランダム**：指定STEP集合から各問のSTEPを完全ランダムに引く（均等配分しない）。
